type GlobalStyleComponentPropTypes = {
  theme: {
    text: string;
    textAccent: string;
    primary: string;
    secondary: string;
    tertiary: string;
  };
};

export default GlobalStyleComponentPropTypes;
